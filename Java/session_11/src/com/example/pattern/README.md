# Regex & Pattern Matching

| File | Description |
|---|---|
| [Demo.java](Demo.java) | Demonstrates `String.split()` on whitespace and commas, and `Pattern.matches()` with character classes (`[abc]`, `[^abc]`, `[a-zA-Z]`), wildcards (`.*`), and quantifiers (`+`) |

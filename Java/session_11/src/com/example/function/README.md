# Functional Interface — Function

| File | Description |
|---|---|
| [DemoFunction.java](DemoFunction.java) | Demonstrates `Function<T, R>` with lambda expressions — converts a `String` to combined lower/upper case, and maps a `String` to its `Integer` length via `apply()` |
